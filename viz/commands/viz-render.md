---
description: "Render markdown, Mermaid, or a plan as HTML in the browser"
argument-hint: "[path | plan-name]"
---

Read `skill:/viz-render` and follow it completely. User arguments: $ARGUMENTS

Plugin root is the package directory that contains `lib/render.sh` and `skills/` (this file is `commands/viz-render.md`). Set `OMP_PLUGIN_ROOT` to that directory if unset.
