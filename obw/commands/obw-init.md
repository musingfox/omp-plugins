---
description: "Pick a vault, write `.obsidian.yaml`, install starter templates"
---

Read `skill://obw-init` and follow it completely. User arguments: $ARGUMENTS

Plugin root is the package directory that contains `commands/` and `skills/` (this file is `commands/obw-init.md`). Set `OMP_PLUGIN_ROOT` to that directory if unset.
