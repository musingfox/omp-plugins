---
description: "Task / document / ADR lifecycle in the Obsidian vault"
argument-hint: "[intent]"
---

Read `skill://obw-pm` and follow it completely. User arguments: $ARGUMENTS

Plugin root is the package directory that contains `commands/` and `skills/` (this file is `commands/obw-pm.md`). Set `OMP_PLUGIN_ROOT` to that directory. Do not use Claude Code plugin paths.
